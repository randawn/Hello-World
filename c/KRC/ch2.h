
void print_range(void);                 // ex 2-1
void for_loop(void);                    // ex 2-2
int  htoi(char s[]);                    // ex 2-3
int  lenLine(char line[]);
int  htoichar(char h);
void squeeze(char* s1, char* s2);       // ex 2-4
// FIXME: in Python, strings are supposed to be immutable!
// violate this will receive a vast amount of wrath when unleash
int any(char * s1, char * s2);          // ex 2-5
unsigned setbit(unsigned x, int p, int n, unsigned y); // ex 2-6
void hexPbin(unsigned x);
unsigned invert(unsigned x, int p, int n);        // ex 2-7
unsigned rightrot(unsigned x, int n);             // ex 2-8
int bitcount(unsigned x);               // ex 2-9
char lower(char c);                     // ex 2-10
