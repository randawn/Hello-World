#include <stdio.h>
#include "ch2.h"

int main(void)
{
    return 0;
}


/* Exercise 2-1
 * Write a program to determine the ranges of char, short, int,
 * and long variables, both signed and unsigned,
 * by printing appropriate values from standard headers and by direct computation.
 * Harder if you compute them:
 * determine the ranges of the various floating-point types. 
*/
void print_range()
{
    /*
    char    char_var;
    short   short_var;
    int     int_var;
    long    long_var;
    unsigned char    uchar_var;
    unsigned short   ushort_var;
    unsigned int     uint_var;
    unsigned long    ulong_var;
    printf("the range of char %d - %d", (0x1 | 0x1<<sizeof(char), ~(0x1<<sizeof(char)));
    */
}
