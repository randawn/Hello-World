
(define (fact n)
    (if (= n 1)
        1
        (* n (fact (- n 1)))
    )
)

(fact 3)

(define (fact cum count)
    (if (= count 0)
        cum
        (fact (* cum count) (- count 1))
    )
)

(fact 1 3)

